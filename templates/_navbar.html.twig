<nav class="navbar navbar-expand-lg navbar-light bg-secondary ">
    <div class="container-fluid">
        <!-- Logo -->
        <div class="d-flex align-items-center">
            <a href="{{ path('main_home') }}">
                <img src="{{ asset('img/sortir-logo-blue.svg') }}" alt="logo" style="width: 50%; height: 50%;">
            </a>
        </div>

        <!-- Bouton toggle -->
        <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation -->
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item ">
                    <a class="nav-link text-light" href="{{ path('main_home') }}">Accueil</a>
                </li>
                {% if is_granted ('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('ville_list') }}">Villes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('main_home') }}">Campus</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('app_inscription') }}">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('app_participants_list') }}">Liste des inscrits</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link text-light" href="{{ path('app_login') }}">Se deconnecter</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-light" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            </ul>
        </div>
    </div>

</nav>
<div class="container-fluid d-flex">
    <div>
        <p class="mb-0 mt-2">
            {% if app.user %}
                Bonjour {{ app.user.prenom }} ! Nous somme le :
            {% else %}
                Vous n'êtes pas connecté
            {% endif %}
            {{ "now"|format_date('long', locale='fr') }}
        </p>
    </div>


</div>
